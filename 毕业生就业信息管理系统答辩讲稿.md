# 毕业生就业信息管理系统答辩讲稿

## 一、项目介绍

各位老师好，我的项目名称是“毕业生就业信息管理系统”。

本系统主要面向学校毕业生和就业办工作人员，解决毕业生就业信息录入、工作人员审核以及就业数据统计的问题。

系统采用 Python Flask 开发，使用 SQLite 保存数据，主要实现了学生端和工作人员端两类功能。

## 二、项目背景和建设目标

在实际工作中，毕业生就业信息通常涉及就业时间、就业单位、岗位以及档案发往地等多个字段。

如果使用纸质表格或者多个 Excel 文件进行管理，容易出现信息重复、数据遗漏以及统计不及时等问题。

因此，本项目希望建立一个统一的 Web 管理系统。

学生可以在线注册、登录并填写自己的就业信息；就业办工作人员可以审核这些信息，并对就业数据进行查询和统计。

## 三、系统角色和权限

系统主要包含两类用户。

第一类是学生用户。学生可以注册账号，填写个人学籍信息，查看自己的基本资料，录入和修改就业信息，并查看审核结果。

第二类是就业办工作人员。工作人员可以查看所有学生的就业记录，根据姓名、学号和审核状态进行查询，还可以审核就业信息并填写审核意见。

系统通过角色字段区分学生和工作人员。

学生只能访问自己的信息，不能查看其他学生的数据；工作人员可以访问审核和统计功能。

## 四、系统技术架构

系统整体采用 Flask 单体架构。

浏览器负责显示页面和提交表单，Flask 负责接收请求，并根据不同的 URL 调用相应的处理函数。

在数据访问方面，项目使用 Flask-SQLAlchemy 操作数据库。这样可以通过 Python 类表示数据库表，而不需要在每个地方手动编写 SQL 语句。

页面使用 Jinja2 模板生成。例如，后端查询到学生的姓名和就业单位之后，会把这些数据传给模板，模板再生成最终的 HTML 页面。

数据库使用 SQLite。SQLite 不需要单独安装数据库服务器，适合课程设计和本地演示。

## 五、数据库设计

系统主要设计了三张表。

第一张是用户表，保存账号、密码哈希、角色以及对应的毕业生信息。

第二张是毕业生信息表，保存学号、姓名、专业、班级和联系电话。

第三张是就业信息表，保存就业时间、就业单位、单位性质、岗位和档案发往地。

毕业生和就业信息之间是一对一关系，也就是一个毕业生最多对应一条就业记录。

就业记录中还有一个审核状态字段，主要包括三种状态：

```text
pending：待审核
approved：审核通过
rejected：审核驳回
```

通过这个状态字段，可以清楚地记录就业信息当前所处的处理阶段。

## 六、系统业务流程

系统的主要业务流程可以概括为五步。

第一步，学生注册账号并登录系统。

第二步，学生填写就业时间、就业单位、单位性质、工作岗位和档案发往地。

第三步，学生保存信息后，系统自动将状态设置为“待审核”。

第四步，就业办工作人员登录后台，对学生提交的信息进行审核，可以选择通过或者驳回，并填写审核意见。

第五步，工作人员可以在统计页面查看总记录数、待审核数量、审核通过数量和已驳回数量。

如果学生修改已经审核过的信息，系统会重新把状态设置为“待审核”，这样可以保证修改后的内容必须重新审核。

## 七、学生端功能

学生登录之后进入个人中心。

页面上方显示学生的学号、姓名、专业、班级和联系电话。

如果学生还没有就业记录，可以点击“录入就业信息”，填写就业时间、单位名称、单位性质、岗位和档案发往地。

如果已经有就业记录，可以点击“修改”，更新自己的信息。

保存之后，系统会提示“就业信息已保存，等待审核”，并且状态显示为待审核。

学生只能查看与自己账号关联的毕业生信息，不能访问工作人员的审核页面。

## 八、工作人员端功能

工作人员登录后进入就业信息审核页面。

页面中会列出学生的学号、姓名、就业单位和当前审核状态。

工作人员可以通过姓名、学号或者审核状态进行筛选。

点击某条记录后，可以查看具体就业信息，并填写审核意见。

审核结果可以选择“审核通过”或者“驳回”。

审核完成后，系统会记录审核状态、审核意见和审核时间。

除此之外，工作人员还可以访问统计页面，查看不同审核状态的记录数量，以及不同单位性质对应的就业人数。

## 九、系统运行方式

系统运行比较简单。

首先安装项目依赖：

```bash
pip install -r requirements.txt
```

然后初始化演示数据：

```bash
flask --app app init-demo
```

最后启动 Flask 应用：

```bash
python app.py
```

启动以后，在浏览器中访问：

```text
http://127.0.0.1:5000
```

项目提供了两个演示账号。

学生账号：

```text
student001 / 123456
```

工作人员账号：

```text
admin / admin123
```

此外，系统也支持学生自行注册新账号。

## 十、项目总结

通过本项目，我完成了一个基本的毕业生就业信息管理系统。

系统实现了用户注册登录、角色权限控制、就业信息录入、就业信息修改、工作人员审核和就业数据统计等功能。

技术上使用了 Python、Flask、SQLite、SQLAlchemy、Jinja2、HTML 和 CSS。

这个项目目前是一个课程演示版本，重点是展示完整的业务流程和系统结构。

如果继续扩展，可以增加文件上传、Excel 导出、数据可视化、管理员账号管理、短信通知以及 MySQL 数据库支持等功能。

我的介绍到这里，谢谢各位老师。下面可以进行系统演示和提问。
